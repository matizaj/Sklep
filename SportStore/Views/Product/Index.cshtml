@model ProductListViewModel


@foreach (var p in Model.Products)
{
    @Html.Partial("ProductSummaryPartial", p)    
}

<div page-model="@Model.PageInfo" 
     page-action="Index" 
     page-url-category="@ViewBag.SelectedCat" 
     page-class-enabled="true" 
     page-class="btn" 
     page-class-normal="btn-secondary" 
     page-class-selected="btn-primary"
     class="btn-group mr-2 ml-5"></div>
<a asp-controller="Product" asp-action="Create" class="btn btn-primary ml-0">Dodaj produkt</a>
